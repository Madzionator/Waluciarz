<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="Waluciarz.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:views="clr-namespace:Waluciarz.MVVM.Views"
    Shell.FlyoutBehavior="Disabled">

    <ShellContent
        Title="Home"
        ContentTemplate="{DataTemplate views:HomeView}"
        Route="HomeView" />

    <ShellContent
        Title="ExchangeRate"
        ContentTemplate="{DataTemplate views:ExchangeRateView}"
        Route="ExchangeRate" />

    <ShellContent
        Title="WorldCurrencies"
        ContentTemplate="{DataTemplate views:NewsView}"
        Route="WorldCurrencies" />

</Shell>